// include para busca valores dentro de un arreglo
let numbers = [1, 2, 3, 4, 5, 6, 7, 8];
if(numbers.includes(7)) {
    console.log('Si se encuentra el valor 7');
} else {
    console.log('no se encuetra el valor.');
}

// exponenciales
let base = 4;
let exponent = 3;
let result = base ** exponent;
console.log(result);